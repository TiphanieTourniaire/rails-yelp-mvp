<h1>All restaurants</h1>
<ul>
  <% @restaurants.each do |r| %>
<li>
  <%= link_to r.name, restaurant_path(r) %>
  <%= r.category %>: <%= r.address %> (<%= r.phone_number %>)
</li>
<% end %>
</ul>

<p><%= link_to "Submit a new restaurant", new_restaurant_path %></p>